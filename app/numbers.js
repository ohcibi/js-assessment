exports = typeof window === 'undefined' ? global : window;

exports.numbersAnswers = {
  valueAtBit(num, bit) {

  },

  base10(str) {

  },

  convertToBinary(num) {

  },

  multiply(a, b) {

  },
};
